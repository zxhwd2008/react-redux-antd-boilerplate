import { createState } from '../utils/createState'
import { constructMandrillTemplateData } from '../utils/mandrillUtils'

export const ComponentsGallery = createState({
  name: 'ComponentsGallery',
  fields: {
  },
})
